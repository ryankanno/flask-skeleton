upstream flask_skeleton {
    server 127.0.0.1:50060;
}

server {
    listen 80;
    server_name flask_skeleton.local;
    charset utf-8;
    root {{ application.target.path }}/current;

    location /static {
        alias {{ application.target.static_path }};
    }

    location / {
        uwsgi_pass  flask_skeleton;
        include     uwsgi_params;
    }
}
